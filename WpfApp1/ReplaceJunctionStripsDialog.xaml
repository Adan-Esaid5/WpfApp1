<Window x:Class="WpfApp1.ReplaceJunctionStripsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="החלפת רצועות לצומת" Height="300" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="#F0F0F0">

    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="HeaderStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,10,0,15"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
        </Style>

        <Style x:Key="ComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5,2"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="החלפת רצועות לצומת:" 
                   Style="{StaticResource HeaderStyle}"/>

        <!-- Replacement Options -->
        <Border Grid.Row="1" 
                BorderBrush="#CCCCCC" 
                BorderThickness="1" 
                Padding="15" 
                Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Replace Road Row -->
                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="החלפת דרך:" 
                           Style="{StaticResource LabelStyle}"/>

                <ComboBox Grid.Row="0" Grid.Column="1" 
                          x:Name="ReplaceRoadComboBox"
                          Style="{StaticResource ComboBoxStyle}"
                          SelectionChanged="RoadComboBox_SelectionChanged"/>

                <TextBlock Grid.Row="0" Grid.Column="2" 
                           Text="כיוון:" 
                           Style="{StaticResource LabelStyle}"/>

                <ComboBox Grid.Row="0" Grid.Column="3" 
                          x:Name="ReplaceDirectionComboBox"
                          Style="{StaticResource ComboBoxStyle}"/>

                <!-- With Road Row -->
                <TextBlock Grid.Row="1" Grid.Column="0" 
                           Text="בדרך:" 
                           Style="{StaticResource LabelStyle}"/>

                <ComboBox Grid.Row="1" Grid.Column="1" 
                          x:Name="WithRoadComboBox"
                          Style="{StaticResource ComboBoxStyle}"
                          SelectionChanged="RoadComboBox_SelectionChanged"/>

                <TextBlock Grid.Row="1" Grid.Column="2" 
                           Text="כיוון:" 
                           Style="{StaticResource LabelStyle}"/>

                <ComboBox Grid.Row="1" Grid.Column="3" 
                          x:Name="WithDirectionComboBox"
                          Style="{StaticResource ComboBoxStyle}"/>
            </Grid>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button x:Name="ReplaceButton" 
                    Content="החלף" 
                    Style="{StaticResource ButtonStyle}" 
                    Click="ReplaceButton_Click"/>

            <Button x:Name="CloseButton" 
                    Content="סגור" 
                    Style="{StaticResource ButtonStyle}" 
                    Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>