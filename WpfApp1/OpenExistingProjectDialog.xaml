<Window x:Class="WpfApp1.OpenExistingProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="פתיחת פרויקט קיים" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        FlowDirection="RightToLeft"
        Background="#F0F0F0">

    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="120"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="0,15,0,10"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="DataGridColumnHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="DataGridCellStyle" TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderBrush" Value="#DDDDDD"/>
            <Setter Property="BorderThickness" Value="0,0,1,1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Projects DataGrid -->
        <DataGrid Grid.Row="0" 
                  x:Name="ProjectsDataGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeRows="False"
                  CanUserSortColumns="True"
                  AlternatingRowBackground="#F5F5F5"
                  RowBackground="White"
                  BorderBrush="#AAAAAA"
                  BorderThickness="1"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}"
                  CellStyle="{StaticResource DataGridCellStyle}"
                  RowHeight="30"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  IsReadOnly="True"
                  MouseDoubleClick="ProjectsDataGrid_MouseDoubleClick">

            <DataGrid.Columns>
                <DataGridTextColumn Header="קוד פרויקט" Width="100" 
                                    Binding="{Binding ProjectCode}"/>

                <DataGridTextColumn Header="מס' צומת" Width="100" 
                                    Binding="{Binding JunctionNumber}"/>

                <DataGridTextColumn Header="שם הצומת" Width="*" 
                                    Binding="{Binding JunctionName}"/>

                <DataGridTextColumn Header="לקוח" Width="150" 
                                    Binding="{Binding Client}"/>

                <DataGridTextColumn Header="תאריך ספירה" Width="120" 
                                    Binding="{Binding CountDate, StringFormat=dd/MM/yyyy}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Open Button -->
        <Button Grid.Row="1" 
                x:Name="OpenProjectButton"
                Content="פתח פרויקט" 
                Style="{StaticResource ButtonStyle}" 
                HorizontalAlignment="Center"
                Click="OpenProjectButton_Click"
                IsEnabled="{Binding ElementName=ProjectsDataGrid, Path=SelectedItems.Count}"/>
    </Grid>
</Window>